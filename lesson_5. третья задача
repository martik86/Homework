"""Написать функцию, принимающую число и возвращающую ближайший к этому числу палиндром.
11 -> 22
188 -> 191
191 -> 202
2541 -> 2552"""

def nearest_palindrome(num):
    while str(num) != str(num)[::-1]:
        num += 1
        return num


print(nearest_palindrome(1233))
